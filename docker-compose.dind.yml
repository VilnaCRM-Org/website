# Docker Compose override file for Docker-in-Docker (DIND) mode
# This file adds predictable container names for DIND container management
# Use with: docker-compose.yml (development) or docker-compose.test.yml (testing)

services:
  dev:
    container_name: website-dev
    networks:
      - website-network

