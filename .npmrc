; Fail `pnpm install` outright when the running Node is outside package.json
; "engines". Without this the field is advisory, which is how four divergent
; Node pins (.nvmrc, Dockerfiles, vars.NODE_VERSION, engines) drifted apart.
engine-strict=true
