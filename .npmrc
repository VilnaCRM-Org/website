; Fail installs outright when the running Node is outside package.json
; "engines" (also asserted by checkNodeVersion.js on `make install`). Without
; this the field is advisory, which is how four divergent Node pins (.nvmrc,
; Dockerfiles, vars.NODE_VERSION, engines) drifted apart.
engine-strict=true
