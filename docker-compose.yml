services:
  dev:
    build: .
    command: make start CI=1
    restart: unless-stopped
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=development
