set -e

# Fast, staged-file scope: Prettier + ESLint only on what is about to land.
bun x lint-staged

# Types are non-local (a change in one file can break another), so the type
# check runs over the whole project. CI=1 runs the host toolchain directly, so
# the hook works without the Docker dev stack.
CI=1 make lint-tsc
